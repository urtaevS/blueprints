
This blueprint lets you manage a Home Assistant **todo** list через Telegram: добавлять пункты обычными сообщениями, отмечать выполненные, переключать режимы отображения и очищать список.

## Описание

- Добавление пунктов в сущность `todo` из обычных текстовых сообщений Telegram:  
  - Поддерживается ввод через запятую на одной строке: `молоко, хлеб, яйца`.  
  - Blueprint разбивает строку на элементы, обрезает пробелы и добавляет только новые позиции (без дубликатов по имени, регистр не учитывается).  
- Отображение списка одним сообщением с inline‑кнопками:  
  - Нажатие `✅ Название` помечает пункт как выполненный (`completed`).  
  - Переключение между режимами:
    - «🛒 Только активные» — только незавершённые (`needs_action`).  
    - «📜 Показать все» — активные + выполненные (история).  
  - Кнопка `🗑 Очистить завершенные` удаляет все завершённые задачи из списка.  
- Постоянная reply‑клавиатура с командами:
  - `📜 Показать список` — открыть список активных пунктов.  
  - `🛒 Только активные` — принудительно активный режим.  
  - `📜 Показать все` — показать активные и завершённые.  
  - `🗑 Очистить завершенные` — удалить все завершённые.  
  - `🔥 Очистить всё` — удалить все пункты выбранного списка `todo`.  
- Чистый чат: каждое сообщение‑команда с reply‑клавиатуры удаляется сразу после обработки.  
- Inline‑сообщение со списком переиспользуется: blueprint хранит `message_id` последнего сообщения и редактирует его вместо создания новых.  

---

## Требования перед запуском

Перед импортом и запуском убедитесь, что выполнены следующие шаги.

### 1. Обновить Home Assistant

- Используйте версию **Home Assistant 2025.11+**.  
- Blueprint опирается на:
  - новый синтаксис `actions:` в автоматизациях;  
  - сервисы `todo.get_items`, `todo.add_item`, `todo.update_item`, `todo.remove_completed_items`, `todo.remove_item`.  

### 2. Настроить интеграцию Telegram‑бота

1. Добавьте официальную интеграцию **Telegram bot** через  
   `Настройки → Устройства и службы (Settings → Devices & Services)`.  
2. Убедитесь, что:
   - у вас есть рабочий токен бота;  
   - есть чат (личный или групповой), где бот может писать сообщения.  
3. Определите **Config Entry ID** этого бота:
   - создайте тестовую автоматизацию с действием `telegram_bot.send_message`;  
   - в настройках действия будет указан Config Entry ID (например: `01KC736CV60QW39DX3A4FHNW0K`).  

### 3. Создать или выбрать список дел (todo‑entity)

- Убедитесь, что есть хотя бы одна сущность `todo`, например:
  - встроенный список покупок `todo.shopping_list`;  
  - или любая другая `todo.*`‑сущность.  

### 4. Создать helper для хранения `message_id`

Создайте в Home Assistant один helper:

- **Рекомендуется** `input_text` (строковый)  
- Либо `input_number` (числовой)  

Этот helper будет хранить `message_id` последнего inline‑сообщения со списком, чтобы blueprint мог:

- редактировать существующее сообщение;  
- не засорять чат новыми сообщениями при каждом изменении списка.  

### 5. Спланировать текст reply‑клавиатуры

Blueprint ожидает **русский текст внутри логики**, но reply‑клавиатура основана на конкретных строках:

- `📜 Показать список`  
- `🛒 Только активные`  
- `📜 Показать все`  
- `🗑 Очистить завершенные`  
- `🔥 Очистить всё`  

Важно использовать **точно такие строки**, чтобы фильтр команд в разделе `telegram_text` работал корректно.

***

## Настройка после импорта

### 1. Импортировать blueprint

1. Добавьте YAML из README в раздел **Blueprints**.  
2. После импорта blueprint появится в:  
   `Настройки → Автоматизации и сцены → Чертежи (Settings → Automations & Scenes → Blueprints)`.

### 2. Создать автоматизацию по blueprint’у

При создании автоматизации по этому blueprint’у нужно заполнить:

- **To‑do список (`todo_entity`)**  
  Сущность `todo`, которой нужно управлять, например:  
  - `todo.shopping_list`.  

- **Telegram chat_id (`telegram_chat_id`)**  
  Числовой ID чата, где бот будет принимать и отправлять сообщения:  
  - можно узнать через бота `@userinfobot` или логирование событий.  

- **Команда для списка (`list_command`)**  
  Telegram‑команда, открывающая список; по умолчанию:  
  - `/list`  
  Работает аналогично кнопке **«🛒 Только активные»**.  

- **Config Entry ID бота (`bot_config_entry_id`)**  
  Строка ID конфигурации интеграции `telegram_bot`, полученная на шаге настройки бота.  

- **Helper для хранения message_id (`last_message_id_helper`)**  
  Ранее созданный `input_text` или `input_number`, в котором будет храниться ID последнего inline‑сообщения.  

Сохраните и **включите** автоматизацию.

***

## Создание reply‑клавиатуры (однократно)

Запуск клавиатуры через 
Панель разработчика -> Действия:

```yaml
action: telegram_bot.send_message
data:
  config_entry_id: ВАШ_CONFIG_ENTRY_ID
  target: ВАШ_CHAT_ID
  keyboard:
    - "📜 Показать список"
    - "🛒 Только активные"
    - "📜 Показать все"
    - "🗑 Очистить завершенные"
    - "🔥 Очистить всё"
  message: "Клавиатура инициализирована"

```

- Замените `ВАШ_CONFIG_ENTRY_ID` на ID конфигурации бота.  
- Замените `ВАШ_CHAT_ID` на ID чата.  
- Запустите этот скрипт/действие **один раз** — клавиатура останется закреплённой в чате.

---

## Как использовать

### Добавление пунктов

- Отправьте в чат обычное сообщение, например:  
  - `молоко, хлеб, яйца`  
- Blueprint:
  - разделит строку по запятым;  
  - обрежет пробелы;  
  - добавит только новые пункты в выбранную сущность `todo` (без дубликатов по имени).  

### Показать список (активные)

- Используйте:
  - команду `/list`, **или**  
  - кнопку reply‑клавиатуры `📜 Показать список` / `🛒 Только активные`.  
- В ответ придёт сообщение «Ваш список покупок:» с inline‑кнопками `✅ Название`.

### Переключение режимов: активные ↔ все

В inline‑сообщении:

- Нажмите `📜 Показать все`, чтобы открыть историю (активные + выполненные).  
- Нажмите `🛒 Только активные`, чтобы вернуться к списку только незавершённых.  

### Отметить пункт выполненным

- Нажмите на кнопку `✅ Название`.  
- Пункт получает статус `completed` в сущности `todo` и пропадает из вида «Только активные».  

### Повторно активировать выполненный пункт

- В режиме «📜 Показать все» выполненные пункты отображаются как `☑️ Название`.  
- Нажмите на `☑️ Название`, чтобы вернуть пункт в статус `needs_action` (снова активный) и видеть его в списке активных.  

### Очистить выполненные

- Нажмите:
  - inline‑кнопку `🗑 Очистить завершенные`, **или**  
  - команду reply‑клавиатуры `🗑 Очистить завершенные`.  
- Все завершённые элементы будут удалены из списка `todo`.  

### Очистить весь список

- Нажмите `🔥 Очистить всё` в reply‑клавиатуре.  
- Все элементы выбранной сущности `todo` будут удалены.  

***

## Примечания

- Blueprint хранит `message_id` последнего inline‑сообщения в helper-е и при каждом изменении списка **редактирует существующее сообщение**, а не создаёт новое.  
- Сообщения с текстом команд reply‑клавиатуры удаляются (через `telegram_bot.delete_message`), что позволяет поддерживать чат в аккуратном состоянии.
